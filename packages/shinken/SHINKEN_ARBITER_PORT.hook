#!/bin/bash

case "$1" in
    alias)
        echo "TCP port the Shinken arbiter listens on"
    ;;
    description)
        cat << EOF
If you configured Shinken as the monitoring core, this is the
listening port of the arbiter .
EOF
    ;;
    default)
        echo "7770"
    ;;
    choices)
        echo "[0-9]{1,5}"
    ;;
    set)
    ;;
esac
