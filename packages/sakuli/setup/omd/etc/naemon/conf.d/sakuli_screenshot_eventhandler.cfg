define command {
  command_name                   sakuli_screenshot_eventhandler
  command_line                   $USER2$/sakuli_screenshot_eventhandler.sh "$SERVICESTATE$" "$HOSTNAME$" "$SERVICEDESC$" "$LASTSERVICECHECK$" "$SERVICEATTEMPT$"  "$SERVICEEVENTID"
}
