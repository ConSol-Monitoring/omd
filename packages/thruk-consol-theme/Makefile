include ../../Makefile.omd

NAME     = consol-theme


build:

install:
	mkdir -p $(DESTDIR)$(OMD_ROOT)/share/thruk/themes/themes-available/
	cp -rp consol-theme $(DESTDIR)$(OMD_ROOT)/share/thruk/themes/themes-available/Consol

skel:

clean:
