server:
  http_listen_port: ${CONFIG_LOKI_PROMTAIL_PORT}
  grpc_listen_port: 0

positions:
  filename: var/loki/positions.yaml

clients:
  - url: http://127.0.0.1:${CONFIG_LOKI_HTTP_PORT}/loki/api/v1/push

scrape_configs:
  # - start of scrape configs
  # - end of scrape configs
