include ../../Makefile.omd

NAME = omd
VERSION = $(OMD_VERSION)
SHELL = /bin/bash

build:

install:
	# Create directory for admins' local extensions
	mkdir -p $(DESTDIR)$(OMD_ROOT)/local/{bin,share,lib,plugins}

clean:
